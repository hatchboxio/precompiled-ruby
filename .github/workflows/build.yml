name: Build

on:
  workflow_dispatch:
    inputs:
      version:
        required: true
        type: string
  workflow_call:
    inputs:
      version:
        required: true
        type: string

permissions:
  contents: read

env:
  # Where the bootstrap Ruby that runs bin/package inside the manylinux containers comes
  # from. Any repository with a released ruby-3.4.7.<target>.no_yjit.tar.gz will do; switch
  # this to your own once it has one.
  BOOTSTRAP_REPO: jdx/ruby
  BOOTSTRAP_VERSION: "3.4.7"

jobs:
  # What this version's series allows: old series build for Linux only, and get a smoke
  # test their stdlib can pass. See recipes/series.yml.
  plan:
    runs-on: ubuntu-latest
    outputs:
      macos: ${{ steps.info.outputs.macos }}
      legacy: ${{ steps.info.outputs.legacy }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: .ruby-version
      - id: info
        run: bin/recipe-info "${{ inputs.version }}" >> "$GITHUB_OUTPUT"

  build-macos:
    needs: plan
    if: needs.plan.outputs.macos == 'true'
    runs-on: macos-14
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: ruby/setup-ruby@v1
        id: setup-ruby
        with:
          ruby-version: ${{ inputs.version }}

      - name: Install Rust
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --profile minimal --default-toolchain none
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"

      - name: Build portable Ruby
        env:
          JDX_RUBY_BASERUBY: ${{ steps.setup-ruby.outputs.ruby-prefix }}/bin/ruby
          JDX_RUBY_RUSTUP_HOME: ${{ github.workspace }}/.build/rustup-macos
        run: bin/package "${{ inputs.version }}" --target macos --yjit --output rubies

      - name: Upload portable Ruby
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ruby-${{ inputs.version }}-macos-yjit-true
          path: rubies/

      - name: Test portable Ruby
        run: |
          mkdir -p portable-ruby
          tar --strip-components 1 -C portable-ruby -xf rubies/*.tar.gz
          export PATH="${PWD}/portable-ruby/bin:${PATH}"
          ruby --version --yjit
          ruby -r debug -r bigdecimal -r json -r yaml -r reline -e "true"

  build-linux:
    needs: plan
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-22.04
            target: x86_64_linux
            yjit: true
          - runner: ubuntu-22.04
            target: x86_64_linux
            yjit: false
          - runner: ubuntu-22.04-arm
            target: arm64_linux
            yjit: true
          - runner: ubuntu-22.04-arm
            target: arm64_linux
            yjit: false
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: .ruby-version

      - name: Resolve target container
        id: target
        env:
          TARGET: ${{ matrix.target }}
        run: |
          container=$(ruby -ryaml -e 'puts YAML.load_file("recipes/targets.yml").fetch("targets").fetch(ENV.fetch("TARGET")).fetch("container")')
          echo "container=$container" >> "$GITHUB_OUTPUT"

      - name: Build portable Ruby in manylinux2014
        env:
          VERSION: ${{ inputs.version }}
          TARGET: ${{ matrix.target }}
          YJIT: ${{ matrix.yjit }}
          CONTAINER: ${{ steps.target.outputs.container }}
        run: |
          set -euxo pipefail
          docker pull "$CONTAINER"
          docker run --rm \
            -e VERSION \
            -e TARGET \
            -e YJIT \
            -e BOOTSTRAP_VERSION \
            -e BOOTSTRAP_REPO \
            -v "$PWD:/work" \
            -w /work \
            "$CONTAINER" \
            /bin/bash -lc '
              set -euxo pipefail
              yum install -y curl tar gzip xz make gcc gcc-c++ perl perl-IPC-Cmd perl-Time-Piece patch file findutils diffutils which git binutils

              bootstrap_version="$BOOTSTRAP_VERSION"
              bootstrap_root=".build/bootstrap/${TARGET}-${bootstrap_version}"
              if [[ ! -x "${bootstrap_root}/bin/ruby" ]]; then
                case "$TARGET" in
                  x86_64_linux) bootstrap_platform="x86_64_linux" ;;
                  arm64_linux) bootstrap_platform="arm64_linux" ;;
                  *) echo "unknown target: $TARGET" >&2; exit 1 ;;
                esac
                bootstrap_url="https://github.com/${BOOTSTRAP_REPO}/releases/download/${bootstrap_version}/ruby-${bootstrap_version}.${bootstrap_platform}.no_yjit.tar.gz"
                rm -rf "$bootstrap_root"
                mkdir -p "$bootstrap_root"
                curl -fL --retry 3 -o /tmp/bootstrap-ruby.tar.gz "$bootstrap_url"
                tar -xzf /tmp/bootstrap-ruby.tar.gz -C "$bootstrap_root" --strip-components 1
              fi

              export PATH="${PWD}/${bootstrap_root}/bin:${PATH}"
              if [[ "$YJIT" == "true" ]]; then
                curl --proto "=https" --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --profile minimal --default-toolchain none
                export PATH="${HOME}/.cargo/bin:${PATH}"
                export JDX_RUBY_RUSTUP_HOME="${PWD}/.build/rustup-${TARGET}"
                yjit_arg="--yjit"
              else
                yjit_arg="--no-yjit"
              fi

              bin/package "$VERSION" --target "$TARGET" "$yjit_arg" --output rubies
              chown -R "$(stat -c %u /work):$(stat -c %g /work)" rubies .build .cache || true
            '

      - name: Upload portable Ruby
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ruby-${{ inputs.version }}-${{ matrix.target }}-yjit-${{ matrix.yjit }}
          path: rubies/

      - name: Test portable Ruby
        env:
          LEGACY: ${{ needs.plan.outputs.legacy }}
        run: |
          mkdir -p portable-ruby
          tar --strip-components 1 -C portable-ruby -xf rubies/*.tar.gz
          export PATH="${PWD}/portable-ruby/bin:${PATH}"
          if [[ "${{ matrix.yjit }}" == "true" && "$LEGACY" != "true" ]]; then
            ruby --version --yjit
          else
            ruby --version
          fi
          if [[ "$LEGACY" == "true" ]]; then
            # No reline before 2.7, no json before 1.9, and -r debug on 3.0 or older starts
            # the interactive debugger. bin/package already ran the real test suite.
            ruby -r openssl -r yaml -r zlib -r bigdecimal -e "true"
          else
            ruby -r debug -r bigdecimal -r json -r yaml -r reline -e "true"
          fi
